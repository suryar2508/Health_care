"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.validateUser = exports.validatePrescription = exports.validateAppointment = exports.validateRequest = void 0;
const express_validator_1 = require("express-validator");
const appError_1 = require("../utils/appError");
const validateRequest = (req, _res, next) => {
    const errors = (0, express_validator_1.validationResult)(req);
    if (!errors.isEmpty()) {
        throw new appError_1.AppError('Validation failed', 400);
    }
    next();
};
exports.validateRequest = validateRequest;
const validateAppointment = (req, _res, next) => {
    const { doctorId, patientId, date, time } = req.body;
    if (!doctorId || !patientId || !date || !time) {
        throw new appError_1.AppError('Missing required fields', 400);
    }
    next();
};
exports.validateAppointment = validateAppointment;
const validatePrescription = (req, _res, next) => {
    const { patientId, doctorId, medications } = req.body;
    if (!patientId || !doctorId || !medications || !Array.isArray(medications)) {
        throw new appError_1.AppError('Invalid prescription data', 400);
    }
    next();
};
exports.validatePrescription = validatePrescription;
exports.validateUser = [
    body('email').isEmail().withMessage('Invalid email format'),
    body('password').isLength({ min: 6 }).withMessage('Password must be at least 6 characters long'),
    body('name').isString().trim().notEmpty().withMessage('Name is required'),
    body('userType').isIn(['admin', 'doctor', 'patient', 'pharmacist']).withMessage('Invalid user type'),
    (req, res, next) => {
        const errors = (0, express_validator_1.validationResult)(req);
        if (!errors.isEmpty()) {
            return res.status(400).json({ errors: errors.array() });
        }
        next();
    }
];
//# sourceMappingURL=validation.js.map